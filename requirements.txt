# ============================================================================
# Trader AI — Python Dependencies
# Python >= 3.12
# ============================================================================

# -- Twelve Data WebSocket streaming ------------------------------------------
websockets>=12.0

# -- Azure Blob Storage -------------------------------------------------------
azure-storage-blob>=12.19.0

# -- Parquet / columnar storage -----------------------------------------------
pyarrow>=15.0.0
pandas>=2.2.0

# -- Numeric ------------------------------------------------------------------
numpy>=1.26.0

# -- HTTP clients (webhook, Telegram bot, Discord, Dukascopy) -----------------
requests>=2.31.0,<3.0.0

# -- ML: Gradient Boosting ----------------------------------------------------
lightgbm>=4.1.0
xgboost>=2.0.0
scikit-learn>=1.4.0

# -- ML: Deep Learning (LSTM / Transformer) -----------------------------------
# CPU-only index — much smaller download (~200MB vs ~2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0

# -- Serialization ------------------------------------------------------------
joblib>=1.3.0

# -- Environment management ---------------------------------------------------
python-dotenv>=1.0.0

# -- Quotex unofficial API (for reading trade results) -------------------------
git+https://github.com/cleitonleonel/pyquotex.git

# stdlib only (no extra packages needed):
#   asyncio, threading, queue, logging, lzma, struct, json, hmac, hashlib
#   http.server (result callback receiver)
