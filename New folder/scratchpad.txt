CHANGE 6C (addition): Fix Pylance error on line 403

Find this block in _stage_feature_warmup:
            try:
                # Smoke-test: transform the most recent 30 bars.
                _ = engineer.transform(bars_df.tail(30))

Replace with:
            try:
                # Smoke-test: transform the most recent 30 bars.
                assert bars_df is not None, f"No bars for {symbol}"
                _ = engineer.transform(bars_df.tail(30))
